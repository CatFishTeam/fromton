<nav class="navbar navbar-expand-lg navbar-light bg-brown1">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <div class="burgerMenu">
            <i class="fas fa-bars"></i>
            <div class="burgerMenu__links">
                <span class="menuClose">&times;</span>
                <ul>
                    <li><a href="{{ path('home') }}">Accueil</a></li>
                    <li><a href="{{ path('users') }}">Communauté</a></li>
                </ul>
            </div>
        </div>

        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" class="bg-brown2">
        </form>
        <div class="navbar-nav m-auto">
            <a class="navbar-brand text-beige" href="/"><img src="{{ asset('build/assets/images/svg/cheese.svg') }}" alt="mouse">FROMTON</a>
        </div>
        <div class="nav-item">
            <img src="{{ asset('build/assets/images/svg/notification.svg') }}" alt="notification">
        </div>
        {{ notifications()|raw }}
        <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <img src="{{ asset('build/assets/images/svg/mouse.svg') }}" alt="mouse">
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                {%  if not app.user %}
                <a class="dropdown-item" href="{{ path('security_login')}}">Connexion</a>
                {%  endif %}
                {%  if app.user %}
                <a class="dropdown-item" href="{{ path('profil_index', {'id': app.user.id}) }}">Mon compte</a>
                {%  endif %}
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">Something else here</a>
            </div>
        </div>
    </div>
</nav>